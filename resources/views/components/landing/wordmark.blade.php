{{-- The page's signature: the wordmark set larger than the page is wide, then
     cropped along its baseline by the footer slab that follows it. Decorative —
     the accessible name of the site is already in the nav and the <title>. --}}
<div class="overflow-hidden bg-white" aria-hidden="true">
    <p class="landing-wordmark w-full -translate-y-[0.06em] text-center font-jakarta font-extrabold whitespace-nowrap text-ink select-none">
        ADVS
    </p>
</div>
