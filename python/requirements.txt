# ADVS Python ML training/inference dependencies.
#
# NOTE: CLAUDE.md pins TensorFlow 2.15, which only ships wheels up to
# Python 3.11. This venv is Python 3.12, so we use TensorFlow 2.16.x
# (first release with 3.12 wheels). 2.16 defaults to Keras 3 — train_advs.py
# uses only APIs that work under both Keras 2 and 3
# (tf.keras.utils.image_dataset_from_directory, .h5 saving).

# --- core numerics (needed even for make_fixtures.py / dry-run tooling) ---
numpy>=1.26,<2.1
pillow>=10.0
pandas>=2.1

# --- classification / signature / stamp models ---
tensorflow==2.16.*           # use tensorflow-cpu==2.16.* on machines without CUDA
scikit-learn>=1.4
scipy>=1.11
opencv-python-headless>=4.9

# --- detection ---
ultralytics>=8.1             # pulls in torch/torchvision
onnx>=1.15                   # YOLOv8 ONNX export
onnxruntime>=1.17            # ONNX inference sanity checks
