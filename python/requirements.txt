# ADVS Python ML training/inference dependencies.
#
# NOTE: CLAUDE.md pins TensorFlow 2.15, which only ships wheels up to
# Python 3.11. This venv is Python 3.12, so we use TensorFlow 2.16.x
# (first release with 3.12 wheels). 2.16 defaults to Keras 3 — train_advs.py
# uses only APIs that work under both Keras 2 and 3
# (tf.keras.utils.image_dataset_from_directory, .h5 saving).

# --- core numerics (needed even for make_fixtures.py / dry-run tooling) ---
numpy>=1.26,<2.1
pillow>=10.0
pandas>=2.1

# --- classification / signature / stamp models ---
tensorflow==2.16.*           # use tensorflow-cpu==2.16.* on machines without CUDA
scikit-learn>=1.4
scipy>=1.11
opencv-python-headless>=4.9

# --- detection ---
ultralytics>=8.1             # pulls in torch/torchvision
onnx>=1.16,<1.17             # YOLOv8 ONNX export; 1.17+ needs ml_dtypes.float4, which conflicts with TF 2.16's ml-dtypes~=0.3.1
onnxruntime>=1.17            # ONNX inference sanity checks

# --- synthetic training-data generation (template-fill method; classifier) ---
# Renderer = Pillow (already listed above). These add field data + scan realism.
faker>=24.0                  # fake field values (names, TINs, addresses, dates); has an en_PH locale
augraphy>=8.2                # realistic scan/print/photocopy degradation of rendered documents
# NOTE: augraphy depends on opencv-python (GUI build); this repo pins
# opencv-python-headless above. Both can coexist, but if imports clash, keep
# headless and `pip uninstall opencv-python` after installing augraphy.
